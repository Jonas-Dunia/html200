// Create an array with at least 5 strings
let animals = ["cats", "dogs", "birds", "fish", "lizards"];

// Use a for loop to log index and value
for (let i = 0; i < animals.length; i++) {
  console.log((i + 1) + "," + animals[i]);
}